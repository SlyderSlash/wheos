<form method="post" action="{{ path('app_files_categories_delete', {'id': files_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ files_category.id) }}">
    <button class="btn">Delete</button>
</form>
